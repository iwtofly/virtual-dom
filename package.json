{
  "name": "virtual-dom",
  "version": "1.0.0",
  "description": "virtual-dom-test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify build.js -o dist/bundle.js"
  },
  "keywords": [
    "virtual-dom"
  ],
  "author": "iwtofly",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
